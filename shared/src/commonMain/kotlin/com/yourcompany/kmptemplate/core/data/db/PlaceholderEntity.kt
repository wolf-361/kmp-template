package com.yourcompany.kmptemplate.core.data.db

import androidx.room.Entity
import androidx.room.PrimaryKey

// TODO: Remove once real entities are added — exists only to satisfy Room's minimum entity requirement
@Entity(tableName = "placeholder")
data class PlaceholderEntity(@PrimaryKey val id: Int = 0)

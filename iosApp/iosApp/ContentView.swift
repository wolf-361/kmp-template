import SwiftUI
import shared

struct ContentView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("KMP Template")
                .font(.title)
                .fontWeight(.bold)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
